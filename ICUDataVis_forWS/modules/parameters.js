import * as THREE from "three";

//いろんなパラメーター
export const staticParameters = {
  width: 15,
  maxZ: 100,
  center_pos: new THREE.Vector3(0, 0, 0),
  ringRadius :3,
};
export const guiParameters = {
  camMove : true,
  animSpeed : 3,
}